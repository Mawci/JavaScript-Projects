var alertMessage = "HELLO WORLD!!", Sent1 = " I said, ", Sent2 = "\"Don't go to that party\""; //declaring and assigning 3 variables to hold string values with one utilizing the escape character

window.alert(alertMessage);//calling the widow.alert function and passsing the variable alertMessage that holds a string
var num = 3 + 3;//declaring and assigning a variable num that holds the result of an expression 
document.write(num + " ");//calling the document.write function to display to the value the variable is holding from the expression

document.write(Sent1 + Sent2);//calling the document.write fucntion to display and example of combing 2 strings